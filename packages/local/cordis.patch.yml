# dsh-remote-desktop local bundle.
# It replaces the stock workspace browser region with a multi-source browser.
- id: ui-workspace
  disabled: true

- id: ui-settings-general
  disabled: true

- insert:
    - id: remote-desktop
      name: dsh-remote-desktop
