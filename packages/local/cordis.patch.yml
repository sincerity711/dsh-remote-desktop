# dsh-remote-desktop local bundle.
# It shadows the stock workspace browser region with a multi-source browser and extends official Settings slots.
- id: ui-workspace
  disabled: true

- insert:
    - id: remote-desktop
      name: dsh-remote-desktop
