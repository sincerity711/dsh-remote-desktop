# dsh-remote-desktop local bundle.
# It shadows the stock workspace browser region with a multi-source browser.
- id: ui-settings-general
  disabled: true

- insert:
    - id: remote-desktop
      name: dsh-remote-desktop
