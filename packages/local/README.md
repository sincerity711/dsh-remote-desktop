# dsh-remote-desktop

Local dsh plugin that discovers SSH hosts from `~/.ssh/config`, connects remote dsh web instances through SSH, exposes per-host iframe proxies, and replaces the local workspace browser with a project-first local/remote session list. Remote projects show a host badge rather than a source-machine group.
