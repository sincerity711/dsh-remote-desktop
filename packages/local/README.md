# dsh-remote-desktop

Local dsh plugin that connects remote dsh web instances through SSH, exposes a per-source iframe proxy, and replaces the local workspace browser with a local/remote session list.
